# Dependências do SALEIA — Backend FastAPI

# Framework web
# Sem pino de versao exata: claude-agent-sdk -> mcp/sse-starlette exigem
# starlette >=0.48/0.49 no Python 3.14, incompativel com o range de
# starlette exigido pelo fastapi 0.111.0 (<0.38.0) — deixar o resolvedor
# escolher uma versao de fastapi compativel com starlette moderno.
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# Variáveis de ambiente
python-dotenv==1.0.1

# Banco de dados SQLite (ORM)
sqlmodel>=0.0.21

# Driver MySQL (MariaDB/MySQL remoto)
pymysql>=1.0.2
cryptography>=42.0.0

# ─── Provedores de LLM ───────────────────────────────────────
# OpenAI (GPT-4o) — primário
openai>=1.52.0

# Anthropic (Claude 3.5 Sonnet) — fallback 1
anthropic>=0.26.0

# Google (Gemini 1.5 Pro) — fallback 2
google-generativeai>=0.7.0

# Claude Agent SDK — piloto "conta Claude do usuário" (agent/claude_account.py)
# Requer o CLI @anthropic-ai/claude-code instalado no host (npm install -g @anthropic-ai/claude-code)
claude-agent-sdk>=0.2.0

# ─── Autenticação ─────────────────────────────────────────────
bcrypt>=4.0.0
PyJWT>=2.8.0

# ─── Transcrição de áudio ─────────────────────────────────────
groq>=0.9.0

# ─── Embeddings locais (Ollama) ────────────────────────────────
# Já é dependência transitiva de openai/httpx, mas usado diretamente pelo
# OllamaEmbeddingProvider (services/embeddings) — pinado explicitamente.
httpx>=0.27.0
